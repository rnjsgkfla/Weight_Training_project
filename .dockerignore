# 가상환경·캐시 (이미지에 불필요, 빌드 컨텍스트 대폭 축소)
venv/
.venv/
env/
__pycache__/
*.pyc

# git·에디터·OS
.git/
.gitignore
.claude/
.DS_Store
clone-temp/

# 파이프라인 생성물 (이미지 빌드 시 build_references.py 로 재생성)
data/processed/

# 원본 영상: 기준(모범) 데이터 생성에 쓰는 사레레 영상만 포함, 나머지 테스트용 .mov 는 제외
*.mov
!data/raw/sidelateralraise_front_raw.mov
